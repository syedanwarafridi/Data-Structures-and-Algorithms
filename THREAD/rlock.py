"""
    RLock is just a modified version of lock.
    In Lock we cannot acquire lock multiple times
    In R Lock we will be able to acquire lock multiple times
    Syntex will be same
    Developer need Rlock because of they sometimes did mistake with acquire and release methods
"""

